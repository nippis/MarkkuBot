version: "3.3"
services:
  markkubot:
    build: .
    env_file: .env
    depends_on:
    - db
  db:
    image: postgres
    environment:
      POSTGRES_USER: eltsu7
      POSTGRES_PASSWORD: kaljaa
      POSTGRES_DB: markku
    ports:
    - "5432:5432"
    volumes:
    - data-volume:/data/db
volumes:
  data-volume:
